# Hardware reader dependencies

These assemblies are used by `HardwareReader.cs` and are copied into the packaged release by `build_v3.ps1`.

`HardwareReader.exe` is generated locally during the build and is ignored by Git. Do not commit generated executables or replacement binaries without recording their upstream version and license in `THIRD_PARTY_NOTICES.md`.
